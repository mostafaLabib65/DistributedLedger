package network.events;

public enum Events {

    // Process(Client-Server) Events
    ADDRESS,
    BLOCK,
    REQUEST_LEDGER,
    RECEIVE_LEDGER,
    TRANSACTION,
    REQUEST_PUBLICKEYS,
    RECEIVE_PUBLICKEYS,
    PUBLISH_PUBLICKEY;

    // Elections and Consensus
}
