package network.events;

public enum Events {

    // Process(Client-Server) Events
    ADDRESS,
    BLOCK,
    LEDGER,
    TRANSACTION,
    REQUEST_LEDGER,
    RECEIVE_LEDGER,
    BFT_VOTE,
    // Elections and Consensus
}
